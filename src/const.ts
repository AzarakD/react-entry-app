export enum AppRoute {
  Login = '/login',
  Registration = '/registration',
  ChangePassword = '/change-password',
};

export enum AuthStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
};
